# OS
.DS_Store
Thumbs.db

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment & secrets (NEVER commit these)
.env
.env.*
!.env.example

# Local runtime data (tickets, transcripts)
realtime_agent/data/

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Claude Code session
.claude/

# Build / temp
dist/
build/
*.tmp

# Fly deploy script (contains live secrets — keep local)
fly-deploy.sh
